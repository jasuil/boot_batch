package hello;

//@EnableBatchProcessing
public class Schedules {
    /*
    @Autowired
    BatchConfiguration batchConfiguration;
    @Autowired
    public JobBuilderFactory jobBuilderFactory;
    @Autowired
    public StepBuilderFactory stepBuilderFactory;


    @Scheduled(fixedDelay = 5000)
    public void runJob1(JobCompletionNotificationListener listener, Step step1){
        batchConfiguration.importUserJob(listener, step1);
    }

    @Scheduled(fixedDelay = 10000)
    public void runJob2(JobCompletionNotificationListener listener, Step step1){
        batchConfiguration.importUserJob2(listener, step1);
    }

     */
}
